package com.newsagg.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RateLimitConfig {
    // Rate limit values are configured via application.properties
}
